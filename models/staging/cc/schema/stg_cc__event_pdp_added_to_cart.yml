version: 2

models:
  - name: stg_cc__event_pdp_added_to_cart
    description: Derived from an Ahoy Event named custom_event that is published when a user clicks the add to cart button
    columns:
      - name: event_id
        description: Event ID

      - name: visit_id
        description: Visit ID

      - name: occurred_at_utc
        description: Timestamp for when the event was published

      - name: user_id
        description: User ID
