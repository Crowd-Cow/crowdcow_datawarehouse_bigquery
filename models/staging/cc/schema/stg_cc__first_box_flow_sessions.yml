version: 2

models:
  - name: stg_cc__first_box_flow_sessions
    description: First box flow sessions
    columns:
      - name: id
        description: flow session id

      - name: user_id 
        description: user ID

      - name: order_id 
        description: Order ID

      - name: renew_period_type  
        description: renew period type

      - name: created_at_utc
        description: Timestamp for when a record was added to the source database

      - name: updated_at_utc
        description: Timestamp for when a record in the source database was updated

      - name: entered_at_utc
        description: Timestamp for when a record in the source database was updated

      - name: completed_at_utc
        description: Timestamp for when a record in the source database was updated

      - name: exited_at_utc
        description: Timestamp for when a record in the source database was updated
      
      