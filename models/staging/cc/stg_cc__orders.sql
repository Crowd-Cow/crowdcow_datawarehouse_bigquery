with source as (

    select * from {{ source('cc', 'orders') }}

),

renamed as (

    select
        id,
        coolant_weight_in_pounds,
        portion_errors,
        order_contents_last_notified_hash,
        stripe_card_id,
        packing_notes,
        paid_at,
        address_verified_at,
        portions_fulfilled,
        purchase_postage_failed_at,
        additional_coolant_weight_in_pounds,
        parent_order_id,
        order_contents_last_notified_at,
        prepick_at,
        updated_address_at,
        created_by_subscription_id,
        handling_fee_cents,
        cancelled_by_user_id,
        confirmation_sent_at,
        processing_fee_cents,
        delivery_city,
        original_url,
        subscription_id,
        cancelled_at,
        confirmed_at,
        first_stuck_at,
        print_pick_label_last_attempted_at,
        delivery_locked_at,
        stripe_card_country,
        campaign_id,
        delivery_street_address_2,
        picked_up_at,
        notes_fulfilled_at,
        shipping_fee_cents,
        notes_updated_at,
        fc_id,
        stripe_charge_id,
        stripe_failure_message,
        scheduled_arrival_date,
        print_prepick_label_last_attempted_at,
        force_postage_carrier,
        scheduled_fulfillment_date,
        total_price_cents,
        picked_at,
        stripe_card_exp_year,
        created_by_user_id,
        selected_delivery_method,
        override_postage_service,
        stripe_charge_attempted_at,
        token,
        ahoy_visit_id,
        credit_total_cents,
        day_out_notified_at,
        print_last_attempted_at,
        marked_eligible_to_prepack_at,
        packing_notes_updated_at,
        customer_viewed_at,
        created_at,
        current_state,
        delivery_postal_code,
        user_id,
        notes,
        delivery_latitude,
        print_handwritten_note_last_attempted_at,
        order_identifier,
        confirmation_token,
        sales_tax_cents,
        stripe_card_zip,
        override_postage_carrier,
        delivery_name,
        stripe_card_brand,
        packing_override_at,
        last_changed_at,
        checkout_completed_at,
        session_id,
        order_type,
        packing_wave_id,
        where_order_placed,
        event_id,
        shipments_computed_at,
        next_box_notified_at,
        gift_recipient_id,
        logged_google_analytics_conversion_at,
        phone_number_id,
        delivery_street_address_1,
        update_payment_and_shipping_last_attempted_at,
        updated_at,
        scheduled_fulfillment_date_override_note,
        bids_count,
        packer_id,
        postage_label_note,
        packed_at,
        stripe_card_exp_month,
        stripe_card_id_last_updated_at,
        stripe_card_fingerprint,
        delivery_state,
        stripe_failure_code,
        bid_id,
        total_weight_in_pounds,
        delivery_longitude,
        stripe_card_funding,
        updated_payment_info_at,
        delivery_elevation,
        set_aside_at,
        scheduled_fulfillment_date_override_by_user_id,
        stripe_card_last4,
        _fivetran_deleted,
        delivery_address_is_residential,
        easypost_skip_address_verification,
        silence_delivery_notifications,
        first_order_ever,
        ignore_weather_alerts,
        easypost_requires_signature,
        use_as_default_address,
        recurring,
        _fivetran_synced,
        earliest_to_charge_at

    from source

)

select * from renamed
